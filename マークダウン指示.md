引用ブロックで書いて。

後から足したメモは、本文と分けるために行頭 `>` の引用にする。誤情報の訂正・注意だけ `> [!WARNING]` を足してよい。

| やり方 | 見た目 | 向く用途 |
| --- | --- | --- |
| `> 引用`（いま使っている） | 行頭の `>`。プレビューは左線 | 本文と「後から足した」を分ける |
| `> [!WARNING]` など | 上と同じ＋種類名。プレビューは色枠 | 誤情報の訂正、注意 |
| `---` の区切り線 | 横線 | メモをブロックごと切り離す |
| `**太字**` の見出し行 | 編集中も目立つ | 短い一言だけ強調 |
