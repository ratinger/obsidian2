# 細井研 PC 配線図

最終更新: 2026-06-27

## 概要

細井研で使っている PC 周辺機器の接続関係。USB は Type-A / Type-C を区別してメモ。

## 配線図 プラン A

※ HDMI でモニター接続。音声はモニター内蔵スピーカーから出力。

```mermaid
flowchart TB
    subgraph 入力
        Mic["マイク"]
        Mouse["マウス<br/>（Bluetooth）"]
    end

    subgraph カメラ
        Stand["台（固定）"]
        Cam["カメラ"]
        Stand --- Cam
    end

    subgraph Pixel周辺
        Phone["Pixel<br/>（Type-C ×1）"]
        PhoneHub["拡張コネクタ"]
        Earphone["イヤホン<br/>（Type-C）"]
        Phone -->|USB Type-C| PhoneHub
        PhoneHub -->|USB Type-C| Earphone
    end

    PC["ノート PC<br/>（細井研）"]
    Adapter["アダプター"]
    AC["コンセント 100V"]

    subgraph 出力
        Conn["HDMI 延長コネクタ"]
        Mon["モニター<br/>（HDMI・内蔵スピーカー）"]
        Conn -->|HDMI| Mon
    end

    Mic -->|USB Type-A| PC
    Cam -->|USB Type-A| PC
    Mouse -.->|Bluetooth| PC
    PC -->|HDMI| Conn
    PhoneHub <-->|USB Type-C| PC
    PC <-->|USB Type-C<br/>給電| Adapter
    Adapter -->|100V| AC
```

## 配線図 プラン B

※ HDMI 接続不可の想定。モニターは使わず、Google スピーカーを Bluetooth で接続。

```mermaid
flowchart TB
    subgraph 入力
        Mic["マイク"]
        Mouse["マウス<br/>（Bluetooth）"]
    end

    subgraph カメラ
        Stand["台（固定）"]
        Cam["カメラ"]
        Stand --- Cam
    end

    subgraph Pixel周辺
        Phone["Pixel<br/>（Type-C ×1）"]
        PhoneHub["拡張コネクタ"]
        Earphone["イヤホン<br/>（Type-C）"]
        Phone -->|USB Type-C| PhoneHub
        PhoneHub -->|USB Type-C| Earphone
    end

    PC["ノート PC<br/>（細井研・内蔵画面）"]
    Adapter["アダプター"]
    AC["コンセント 100V"]

    subgraph 出力
        GSpeaker["Google スピーカー<br/>（Bluetooth）"]
    end

    Mic -->|USB Type-A| PC
    Cam -->|USB Type-A| PC
    Mouse -.->|Bluetooth| PC
    GSpeaker -.->|Bluetooth| PC
    PhoneHub <-->|USB Type-C| PC
    PC <-->|USB Type-C<br/>給電| Adapter
    Adapter -->|100V| AC
```
