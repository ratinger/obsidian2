---
name: 5-travel
description: >-
  Plans general trips (hotel, trains, day schedule) and builds Leaflet area maps
  with Google Maps links in the main Markdown. Use when the user runs /5_travel,
  says 5_travel, or wants a travel plan with 見取り図_map.html for an area other
  than the dedicated Nagoya/Yachiyo skills.
---

# 5_travel — 旅行行程＋見取り図

宿泊・移動・日別行程の Markdown と、周辺スポットの Leaflet 見取り図（`_map.html`）を作る。  
名古屋（`/3_nagoya`）・八千代（`/4_yachiyo`）の定型往復は専用スキルを優先する。それ以外の旅行・街歩き用。

## いつ使うか

- `/5_travel` または「5_travel」と言われたとき
- ホテル泊・街歩き・買い物ルートをまとめたいとき
- 本屋・家電・金券・食事などの配置マップが欲しいとき

## 参照テンプレート（書式）

直近の完成例を書式の基準にする（内容のコピペはしない）:

- `14_旅行/20260811_大塚池袋/20260811_大塚池袋.md`
- `14_旅行/20260811_大塚池袋/20260811_大塚見取り図_map.html`
- `14_旅行/20260811_大塚池袋/20260811_池袋見取り図_map.html`

## フェーズ 1: 質問（必須）

未指定なら次を1回で聞く:

1. **日程**（開始日〜終了日）
2. **エリア／拠点**（駅・ホテル）
3. **成果物** — 既定は「行程メモ＋`_map.html`＋Googleマップリンク」
4. **予約情報** — ホテル／特急券など（PDF・スクリーンショット・メモ）
5. **重点スポット種別** — 例: 本屋、家電、金券、食事、観光。省略時はエリアから妥当なものを選ぶ

## フェーズ 2: 調査

Web で確認する（記憶や古いメモだけで断定しない）:

| 調べるもの | 優先ソース |
| --- | --- |
| ホテル・予約 | 予約確認・公式 |
| 列車・特急 | 公式時刻表／予約確認。曜日ダイヤに注意 |
| 店舗の住所・営業時間 | 公式店舗ページ |
| 施設の位置関係 | 公式アクセス文＋地図 |
| 地図ピン座標 | **OpenStreetMap（Overpass 等）の施設・建物中心を優先**。無い場合のみ住所から概位とし、ポップアップに「概位」と書く |

**位置の注意**

- 「駅直結」「百貨店内」と「同ビル／隣接地」を混同しない（例: ヨドバシ池袋は西武店内テナントではない）
- 東西口・南北口の覚え方がある街は、行程メモ冒頭に1行で書く
- 地下街・別館は地上ピンがずれることがある → 南館／本館など建物名で確認

## フェーズ 3: 行程メモ作成

### 出力パス

行程単位のフォルダにまとめる:

`14_旅行/{YYYYMMDD}_{エリア略称}/{YYYYMMDD}_{エリア略称}.md`

- `{YYYYMMDD}` は**旅行開始日**
- 複数日ならタイトルに `YYYYMMDD–DD` や日付範囲を書く
- 見取り図 `_map.html` も**同じフォルダ**に置く

### 必須セクション（この順を基本）

1. **ヘッダ** — 同行者、宿、予約番号、注意（特急券＋乗車券など）
2. **見取り図リンク** — 同フォルダの `_map.html` への相対リンク
3. **0. 予約一覧** — ホテル・列車などの表
4. **0.5 主要ポイント（Googleマップ）** — スポット名と `[地図](https://www.google.com/maps/search/?api=1&query=...)` の表（エリア別に分けてよい）
5. **日別行程** — `| 時刻 | イベント | 注意事項 |`
6. **スポット詳細** — 必要に応じて金券／家電／本屋などの営業時間表
7. **持ち物・注意**

### Googleマップリンクのルール

- **行程メモ本体に書く**（見取り図専用の別 Markdown は作らない）
- クエリは店名・施設の検索で一意になる文言にする
- ホテル表にも `地図` 行を入れてよい

## フェーズ 4: 見取り図 `_map.html`

### 出力パス

エリアごとに1ファイル:

`14_旅行/{YYYYMMDD}_{エリア略称}/{YYYYMMDD}_{エリア}見取り図_map.html`

例: `14_旅行/20260811_大塚池袋/20260811_大塚見取り図_map.html`

### 作るもの／作らないもの

| 作る | 作らない（既定） |
| --- | --- |
| Leaflet + OSM Japan タイルの `_map.html` | 見取り図専用 `.md` |
| | `.png` / `.jpg` 書き出し |
| | `.kml`（ユーザーが明示した場合のみ） |

### `_map.html` の要件

- 既存の `*_見取り図_map.html` をベースに複製して地点だけ差し替える
- カテゴリ色分け（駅／宿／本屋／家電／金券／食事 など）
- ラベル＋ポップアップ（住所・根拠・「概位」注記）
- エリアラベル（東口／西口、北口／南口など）
- `fitBounds` で主要ピンが入る初期表示
- 座標は OSM 確認後に入れる。疑わしいピンは調査し直す

### Leaflet ひな形の要点

```html
<!-- unpkg leaflet 1.9.x / tile: https://{s}.tile.openstreetmap.jp/{z}/{x}/{y}.png -->
<!-- places = [{ name, cat, lat, lng, note? }, ...] -->
<!-- colors by cat; circle markers + text labels; fitBounds -->
```

## 検証チェックリスト（出力前）

- [ ] 日付・曜日と列車ダイヤが一致（土休日まとめにしない）
- [ ] 予約番号・発着・座席を予約確認と照合した（渡されている場合）
- [ ] 主要店の営業時間を公式で確認した（変更注記あり）
- [ ] 地図ピンを OSM 等で見直し、デパート内誤認がない
- [ ] `_map.html` 以外の見取り図ファイルを増やしていない
- [ ] Googleマップリンクが行程メモ本体にある
- [ ] 行程メモ先頭から `_map.html` に辿れる

## 完了報告（日本語・簡潔）

- 作成／更新したファイルパス
- 日程・拠点の要約
- 見取り図エリア一覧
- 未確認事項（在庫・不定休など）があれば1行で
